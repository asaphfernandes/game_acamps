import React from 'react';
import { Link } from 'react-router-dom';

const ResultadoView: React.FC = () => {
    return (<>
        <h1>
            <Link to='/'>Voltar</Link> / Resultado
        </h1>
    </>);
};

export default ResultadoView;
