import styled from "@emotion/styled";

export const ContainerJss = styled('ul')({
    display: 'flex',
    flexDirection: 'column',
    margin: 'auto 20px',
    gap: 16
});

export const ManutencaoJss = styled('li')({
    display: 'flex',
    flexDirection: 'row',
    justifyContent: 'space-between',
    width: 700
});

export const ZerarJss = styled('div')({
    marginTop: 100,
    padding: '100px 16px',
    display: 'flex',
    justifyContent: 'center'
});

export const ResultadoJss = styled('li')({
    display: 'flex',
    flexDirection: 'row',
    justifyContent: 'space-between',
    width: 700
});